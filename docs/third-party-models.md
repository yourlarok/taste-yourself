# 第三方模型与许可证记录

| 模型 | 用途 | 许可证 | 项目选择 |
|---|---|---|---|
| FASHN VTON v1.5 | 静态图像换装 | Apache-2.0；另含上游列出的第三方组件 | 默认自部署候选 |
| CatVTON | 静态/研究 | CC BY-NC-SA 4.0 | 不作为商业内测默认实现 |
| IDM-VTON | 静态/研究 | CC BY-NC-SA 4.0 | 不作为商业内测默认实现 |

许可证记录用于工程筛选，不构成法律意见。每次锁定模型版本、权重和依赖后，都必须保存对应许可证文本和版本标识，并在发布前完成一次合规复核。

FASHN Worker 位于 `workers/fashn_vton/`，通过独立进程隔离重型推理依赖和业务 API。当前安装锁定到上游提交 `7c0f10af3f91ad4048fe9729c470a13ef905d25a`，升级前需重新完成效果、显存和许可证复核。
