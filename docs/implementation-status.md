# 实现状态看板

更新日期：2026-09-25

## 状态规则

本项目不使用主观完成百分比。每项能力只使用以下状态：

- **演示**：交互可走通，但结果来自 Mock 或固定样本。
- **部分可用**：有真实子链路，但完成一次用户任务仍依赖示例数据或人工步骤。
- **已配置待验收**：真实 Provider 已配置，但尚未通过健康检查、真机样本和失败恢复验收。
- **真实可用**：真实数据、真实服务、真机任务及失败恢复均已验收。
- **尚未配置**：代码合同存在，当前环境没有可调用的 Provider。

后端 `GET /api/v1/capabilities` 是运行环境状态的来源，小程序“我的”页直接展示该结果。配置 Provider 不等于服务健康，因此不会自动标为“真实可用”。

## 当前能力矩阵

| 能力 | 代码与数据链路 | 当前默认环境 | 升级为真实可用的验收门槛 |
|---|---|---|---|
| 我的衣橱 | 上传、私有存储、签名读取、最近记录、账号删除已实现 | 真实可用 | 生产对象存储、内容安全和删除任务做一次端到端验收 |
| 静态试穿照 | 小程序取景、本人图上传、任务合同、FASHN VTON Worker、结果保存已实现 | 演示 | 部署模型权重与 GPU；用真实人像/衣服样本验证成功率、耗时、内容安全和超时恢复 |
| 尺码差异 | 中立余量引擎、逐尺码展示、缺数据状态已实现 | 演示/部分可用 | 接入真实人体尺寸；为每件商品导入逐 SKU 成衣胸围、腰围、肩宽、衣长等数据 |
| 尺寸画像 | 正/侧面引导、上传、补拍合同、画像保存与原图删除已实现 | 演示 | 接入经过尺度标定的测量 Provider；完成软尺真值 MAE、P95 和重复性报告 |
| 15 秒动态试衣 | `live-pusher`、`live-player`、会话合同、15 秒上限和降级状态已实现 | 演示或尚未配置 | 接入实时换装/流媒体中继；取得微信类目权限；完成多机型、弱网、切后台与超时验收 |

## 实施顺序

1. **基础环境门**：关闭前端 Mock，连接 HTTPS 后端，完成登录、上传、媒体读取和删除真机闭环。
2. **静态试穿门**：先让最核心的试穿照跑真实模型，并建立样本验收记录。
3. **尺码数据门**：接入人体测量真值和商品 SKU 尺码表；缺任一侧数据时只解释缺口，不猜尺码。
4. **动态试衣门**：在静态链路稳定后接入 15 秒实时服务；任何失败立即回落静态体验。
5. **小范围内测门**：完成隐私、AI 标识、监控、成本上限和回滚开关后，再开放 10–30 人体验版。

## 任务进度呈现

用户任务不显示无法证实的百分比。静态试穿只显示“定格画面 → 上传素材 → 生成试穿照”三个由真实事件推进的阶段；上传衣服继续使用可由客户端获得的字节进度。工程交付则使用上面的验收门，不以“代码文件已存在”代替能力完成。

## 当前外部输入

- 正式 AppID/AppSecret、备案 HTTPS 域名和服务器。
- Ampere 及以上 GPU，或可替代的静态试穿 API 凭据。
- 经标定的人体测量 Provider；必须能返回逐项误差和模型版本。
- 实时换装/流媒体中继账号，以及微信实时音视频所需类目权限。
- 真实商品的逐 SKU 成衣尺寸数据；只有商品标题或用户通常穿着尺码不够。

密钥只进入服务端环境变量，不提交 Git，也不下发到小程序。
